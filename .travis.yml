language: python
sudo: false

cache:
  directories:
    - ~/.cache/pip

python:
  - 3.6

install:
  - python3 mencionar_bot/mencionar_boy.py

script:
  - nosetests --with-coverage

after_success:
  - coveralls
