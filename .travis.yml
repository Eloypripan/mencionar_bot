language: python
sudo: false

cache:
  directories:
    - ~/.cache/pip

python:
  - 3.6

install:
  - ls
  - cd Eloypripan/mencionar_bot
  - python3 mencionar_bot.py

script:
  - nosetests --with-coverage

after_success:
  - coveralls
